Quiero que generes la estructura base de un proyecto llamado “MalamBot” con las siguientes características:

### 1. Tecnologías principales
- El bot está escrito completamente en **TypeScript**, usando:
  - Node.js
  - discord.js v14
  - ESM
  - ts-node-dev para desarrollo
  - Yarn como package manager
- Debe incluir configuración mínima de:
  - tsconfig.json
  - eslint + prettier
  - .env.example

### 2. Arquitectura del bot en TypeScript
Crea una estructura de carpetas como esta:

/src
  /commands
    /utility
    /music
    /rocketleague
  /events
  /core
    client.ts
    logger.ts
  index.ts
  config.ts
  types.ts

### 3. Funcionalidades iniciales (solo placeholders)
- Comando `/ping`
- Comando `/play` (solo placeholder sin integrar audio)
- Comando `/rank` que llama al microservicio en Go (aún sin lógica)
- Evento “ready”
- Evento “interactionCreate”

### 4. Microservicio en Go dentro del mismo proyecto
Dentro del proyecto crea una carpeta:

/microservice
  main.go
  go.mod
  /internal
    /handlers
    /services
    /models

Este microservicio debe:
- Exponer una API REST en el puerto 8080
- Tener un endpoint GET `/rl/rank/{playerId}`
- Usar estructura limpia (handlers, services, models)
- Dejar el handler y servicio como placeholders.

### 5. Comunicación entre TS y Go
En el código TS, deja una función placeholder en:
`/src/rocketleague/fetchRank.ts`
que haga fetch al endpoint del microservicio en Go.

### 6. Scripts en package.json
Agrega:
- "dev": "ts-node-dev --respawn src/index.ts"
- "build": "tsc"
- "start": "node dist/index.js"

### 7. Documentación
Incluye un README básico que explique:
- Cómo instalar
- Cómo iniciar el bot
- Cómo iniciar el microservicio en Go
- Variables necesarias del .env

### 8. Resultado esperado
Devuélveme:
- Toda la estructura de carpetas
- Todos los archivos iniciales con su contenido
- Código bien formateado y listo para usar
- Explicación breve de cada archivo

Genera todo el proyecto completo.